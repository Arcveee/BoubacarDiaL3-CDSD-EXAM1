{% extends 'base.html.twig' %}

{% block title %}Nouveau Menu - Brasil Burger{% endblock %}

{% block body %}
<h1>Nouveau Menu</h1>

{{ form_start(form) }}
    {{ form_row(form.nom) }}
    {{ form_row(form.image) }}
    {{ form_row(form.actif) }}
    {{ form_row(form.burgers) }}
    <button class="btn btn-brasil">Créer</button>
{{ form_end(form) }}

{% endblock %}
