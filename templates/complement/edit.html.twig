{% extends 'base.html.twig' %}

{% block title %}Éditer Complément - Brasil Burger{% endblock %}

{% block body %}
<h1>Éditer Complément</h1>

{{ form_start(form) }}
    {{ form_row(form.nom) }}
    {{ form_row(form.prix) }}
    {{ form_row(form.image) }}
    {{ form_row(form.actif) }}
    <button class="btn btn-brasil">Enregistrer</button>
{{ form_end(form) }}

{% endblock %}
